# Server Environment Variables (NUNCA subir al repositorio)

# Google Sheets API (Solo en el servidor)
GOOGLE_SHEETS_API_KEY=tu_api_key_aqui
GOOGLE_SPREADSHEET_ID=tu_spreadsheet_id_aqui

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Origins (dominios permitidos)
ALLOWED_ORIGINS=https://tu-dominio.com,http://localhost:5173